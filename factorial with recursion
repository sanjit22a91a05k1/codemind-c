// factorial with recursion
# include<stdio.h>
int fac(int );
int main()
{
	int n;
	scanf("%d",&n);
int x=	fac(n);
	printf("%d",x);
}
int fac(int n)
{
	if(n==1)
	{
		return 1;
	}
	else
	{
		return n*fac(n-1);
	}
}
