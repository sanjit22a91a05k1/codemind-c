// factorial of number without recursion
# include<stdio.h>
int fac(int );
int main()
{
	int n;
	scanf("%d",&n);
	int x=fac(n);
	printf("%d",x);
}
int fac(int n)
{
	int fact=1;
	for(int i=1; i<=n; i++)
	{
		fact=fact*i;
	}
	return fact;
}
