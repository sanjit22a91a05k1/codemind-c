// string catcatination 
#include<stdio.h>
int main()
{
	char s1[50],s2[90];
	int i,j;
	gets(s1);
	gets(s2);
	i=strlen(s1);
		for(int j=0; s2[j]!='\0';j++,i++)
		{
			s1[i]=s2[j];
		}
	s1[i]!='\0';
	printf("%s",s1);
}
